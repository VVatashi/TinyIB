{% set title = 'Ban List' %}

{% set breadcrumb = [
  { url: base_url, title: constant('TINYIB_BOARDDESC') },
  { url: base_url ~ '/admin', title: 'Admin Dashboard' },
] %}

{% extends 'admin/layout/_html.twig' %}

{% block content %}
  <div class="list content__list">
    <div class="list__header">
      <h1 class="list__title">{{ title }}</h1>
    </div>

    <div class="list__content">
      <div class="list__actions">
        <ul class="list__actions-list">
          <li class="list__actions-list-item">
            <a class="button" href="{{ base_url }}/admin/bans/create">Add ban</a>
          </li>
        </ul>
      </div>

      {% include 'admin/components/_filters.twig' with filter + {
        'list_url': base_url ~ '/admin/bans',
        'class': 'list__filters',
      } %}

      <div id="list" data-url="{{ base_url }}/ajax/admin/bans">
      {% include 'admin/bans/_list.twig' %}
      </div>
    </div>
  </div>
{% endblock %}
