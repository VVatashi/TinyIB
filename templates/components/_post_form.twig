{% macro markup(id, content) %}
  <tr class="postform__markup-row form__field field">
    <td class="postform__label field__label-wrapper">
    </td>

    <td class="postform__markup-wrapper field__input-wrapper">
      <button type="button" class="button postform__markup field__input field__input_button field__input_markup field__input_markup-{{ id }}" id="markup_{{ id }}">
        {{ content }}
      </button>
    </td>
  </tr>
{% endmacro %}

{% macro field(id, title) %}
  <tr class="postform__field-row form__field form__field_{{ id }} field field_{{ id }}">
    <td class="postform__label field__label-wrapper">
      <label class="field__label field__label_hidden" for="{{ id }}">{{ title }}</label>
    </td>

    <td class="postform__field-wrapper field__input-wrapper">
      <input type="text" class="input postform__input field__input" id="{{ id }}" name="{{ id }}" placeholder="{{ title }}" autocomplete="off" />
    </td>
  </tr>
{% endmacro %}

{% import _self as self %}

<div class="{{ class is defined ? class }}">
  <form class="content__postform postform form form_resizable form_postform" id="postform" name="postform" action="{{ base_url }}/post/create" method="post" enctype="multipart/form-data">
    {% if uploads and constant('TINYIB_MAXKB') > 0 %}
      <input type="hidden" name="MAX_FILE_SIZE" value="{{ constant('TINYIB_MAXKB') * 1024 }}" />
    {% endif %}

    <input type="hidden" name="parent" value="{{ parent }}" />

    {% if constant('TINYIB_CAPTCHA') == 'recaptcha' %}
      {% include 'components/form/_captcha.twig' with {
        'class': [],
        'action': 'create_post',
        'id': 'captcha',
        'name': 'captcha',
        'label': '',
      } %}
    {% endif %}

    {% spaceless %}
      <table class="postform__inner form__inner">
        <tbody class="postform__name-group form__group form__group_inline form__group_name">
          {{ self.field('subject', 'Subject') }}
          {{ self.field('email', 'E-mail') }}
          {{ self.field('name', 'Name') }}

          <tr class="postform__field-row postform__field-row_nostretch form__field form__field_actions field field_actions">
            <td class="postform__label field__label-wrapper"></td>

            <td class="postform__field-wrapper field__input-wrapper">
              <input type="submit" class="button postform__submit field__input field__input_button field__input_submit" value="Submit" />
            </td>
          </tr>
        </tbody>

        <tbody class="postform__markup-group form__group form__group_inline form__group_markup">
          {{ self.markup('b', '<strong>b</strong>') }}
          {{ self.markup('i', '<em>i</em>') }}
          {{ self.markup('u', '<span class="underline">u</span>') }}
          {{ self.markup('s', '<del>s</del>') }}
          {{ self.markup('sup', '<sup>Sup</sup>') }}
          {{ self.markup('sub', '<sub>Sub</sub>') }}
          {{ self.markup('code', '<code>Code</code>') }}
          {{ self.markup('spoiler', '<span class="spoiler">Spoiler</span>') }}
          {{ self.markup('rp', '<span class="spoiler">RP</span>') }}
          {{ self.markup('quote', '<span class="quote">&gt;</span>') }}
        </tbody>

        <tbody class="postform__message-group form__group form__group_inline form__group_message">
          <tr class="postform__message-row form__field form__field_message field field_message">
            <td class="postform__label field__label-wrapper">
              <label class="field__label field__label_hidden" for="message">Message</label>
            </td>

            <td class="postform__message-wrapper field__input-wrapper">
              <textarea class="input postform__message field__input field__input_message" id="message" name="message" placeholder="Message">
              </textarea>
            </td>
          </tr>
        </tbody>

        <tbody class="postform__attachment-group form__group form__group_inline form__group_attachment">
          {% if constant('TINYIB_CAPTCHA') == 'simple' %}
            <tr class="form__field form__field_attachment field field_captcha">
              <td class="postform__label field__label-wrapper">
                <label class="field__label field__label_hidden" for="file">CAPTCHA</label>
              </td>

              <td class="field__input-wrapper">
                {% include 'components/form/_captcha.twig' with {
                  'class': ['form__field form__field_captcha'],
                  'label_class': ['field__label'],
                  'input_class': ['input', 'field__input'],
                  'action': 'create_post',
                  'id': 'captcha',
                  'name': 'captcha',
                  'label': 'CAPTCHA',
                } %}
              </td>
            </tr>
          {% endif %}

          {% if uploads %}
            <tr class="form__field form__field_attachment field field_attachment">
              <td class="postform__label field__label-wrapper">
                <label class="field__label field__label_hidden" for="file">File</label>
              </td>

              <td class="field__input-wrapper">
                <input type="file" class="input field__input" id="file" name="file" placeholder="File" />
              </td>
            </tr>
          {% endif %}

          <tr class="form__field form__field_password field field_password">
            <td class="postform__label field__label-wrapper">
              <label class="field__label field__label_hidden" for="newpostpassword">Password</label>
            </td>

            <td class="field__input-wrapper">
              <input type="text" class="input field__input field__input_password" id="newpostpassword" name="password" placeholder="Delete post password" autocomplete="off" />
            </td>
          </tr>
        </tbody>

        <tbody class="postform__rules-group form__group form__group_rules">
          <tr class="form__field form__field_rules field field_rules">
            <td class="postform__label field__label-wrapper"></td>

            <td class="field__input-wrapper rules">
              <ul class="rules__list">
                {% if constant('TINYIB_REQMOD') == 'files' or constant('TINYIB_REQMOD') == 'all' %}
                  <li class="rules__list-item">
                    All posts {{ constant('TINYIB_REQMOD') == 'files' ? ' with a file attached' }} will be moderated before being shown.
                  </li>
                {% endif %}

                {% if uploads %}
                  <li class="rules__list-item">
                    {{ filetypes }}
                  </li>
                {% endif %}

                {% if uploads and constant('TINYIB_MAXKB') > 0 %}
                  <li class="rules__list-item">
                    Maximum file size allowed is {{ constant('TINYIB_MAXKBDESC') }}.
                  </li>
                {% endif %}

                {% if thumbnails %}
                  <li class="rules__list-item">
                    Images greater than
                    {{ constant('TINYIB_MAXWOP') }}x{{ constant('TINYIB_MAXHOP') }}
                    {% if constant('TINYIB_MAXW') != constant('TINYIB_MAXWOP')
                      or constant('TINYIB_MAXH') != constant('TINYIB_MAXHOP') %}
                      (new thread) or {{ constant('TINYIB_MAXW') }}x{{ constant('TINYIB_MAXH') }} (reply)
                    {% endif %}
                    will be thumbnailed.
                  </li>
                {% endif %}
              </ul>
            </td>
          </tr>
        </tbody>
      </table>

      <div class="postform__right-border"></div>
      <div class="postform__bottom-border"></div>
      <div class="postform__corner"></div>
    {% endspaceless %}
  </form>
</div>
